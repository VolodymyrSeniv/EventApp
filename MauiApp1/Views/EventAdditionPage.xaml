<toolkit:Popup xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
               xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
               xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
               
               xmlns:system="clr-namespace:System;assembly=mscorlib"
               
               x:Class="MauiApp1.Views.EventFormPopup"
               CanBeDismissedByTappingOutsideOfPopup="False">

    <Border StrokeShape="RoundRectangle 20" 
            BackgroundColor="White"
            WidthRequest="320"
            Padding="20">

        <VerticalStackLayout Spacing="15">
            <Label Text="Create New Event" 
                   FontSize="18" 
                   FontAttributes="Bold" 
                   HorizontalOptions="Center"/>

            <Entry x:Name="TitleEntry" Placeholder="Title" />
            <Entry x:Name="LocationEntry" Placeholder="Location" />

            <DatePicker x:Name="EventDatePicker" 
                        MinimumDate="{Binding Source={x:Static system:DateTime.Now}}" />

            <Editor x:Name="DescEntry" Placeholder="Description" HeightRequest="80" />

            <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                <Button Grid.Column="0" 
                        Text="Cancel" 
                        BackgroundColor="#E0E0E0" 
                        TextColor="Black"
                        Clicked="OnCancelClicked"/>

                <Button Grid.Column="1" 
                        Text="Save" 
                        Clicked="OnSaveClicked"/>
            </Grid>
        </VerticalStackLayout>
    </Border>
</toolkit:Popup>