<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ios="clr-namespace:Microsoft.Maui.Controls.PlatformConfiguration.iOSSpecific;assembly=Microsoft.Maui.Controls"
             ios:Page.UseSafeArea="False"
             x:Class="MauiAppB.Views.EditProfilePage"
             NavigationPage.HasNavigationBar="False">

    <Grid>
        <Image Source="bg_main.png" Aspect="AspectFill" HorizontalOptions="Fill" VerticalOptions="Fill"/>

        <Grid RowDefinitions="Auto, *">
            
            <Grid Grid.Row="0" HeightRequest="50" Margin="20, 50, 20, 0">
                <Frame Grid.Column="0" 
                    HeightRequest="40" 
                    WidthRequest="40" 
                    CornerRadius="20" 
                    Padding="0" 
                    BackgroundColor="#80FFFFFF"
                    BorderColor="Transparent" 
                    HasShadow="False"
                    HorizontalOptions="Start">
                <ImageButton Source="powrot.png" 
                         HorizontalOptions="Start" 
                         VerticalOptions="Center" 
                         HeightRequest="40"  
                         WidthRequest="40"
                         Padding="10"
                         BackgroundColor="Transparent"
                         Clicked="OnBackClicked"
                         Margin="-5, 0, 0, 0"/>
            </Frame>
                <Label Text="Edycja profilu" FontSize="20" FontAttributes="Bold" TextColor="Black" HorizontalOptions="Center" VerticalOptions="Center"/>
            </Grid>

            <ScrollView Grid.Row="1" Margin="0, 20, 0, 0">
                <Border Margin="20" Padding="20" BackgroundColor="#95FFFFFF" StrokeShape="RoundRectangle 20" StrokeThickness="0">
                    <VerticalStackLayout Spacing="15">
                        
                        <Frame HeightRequest="100" WidthRequest="100" CornerRadius="50" Padding="0" IsClippedToBounds="True" HorizontalOptions="Center" BorderColor="Transparent">
                            <Image Source="{Binding PhotoUrl}" Aspect="AspectFill"/>
                        </Frame>
                        <Label Text="Zmień zdjęcie" TextColor="Blue" FontSize="12" HorizontalOptions="Center" TextDecorations="Underline"/>

                        <VerticalStackLayout Spacing="5">
                            <Label Text="Imię" FontSize="12" TextColor="Gray"/>
                            <Entry Text="{Binding FirstName}" TextColor="Black" BackgroundColor="White" ReturnType="Done"/>
                        </VerticalStackLayout>

                        <VerticalStackLayout Spacing="5">
                            <Label Text="Nazwisko" FontSize="12" TextColor="Gray"/>
                            <Entry Text="{Binding LastName}" TextColor="Black" BackgroundColor="White" ReturnType="Done"/>
                        </VerticalStackLayout>

                        <VerticalStackLayout Spacing="5">
                            <Label Text="Status" FontSize="12" TextColor="Gray"/>
                            <Entry Text="{Binding Status}" TextColor="Black" BackgroundColor="White" ReturnType="Done"/>
                        </VerticalStackLayout>

                        <VerticalStackLayout Spacing="5">
                            <Label Text="Numer telefonu" FontSize="12" TextColor="Gray"/>
                            <Entry Text="{Binding PhoneNumber}" TextColor="Black" BackgroundColor="White" Keyboard="Telephone" ReturnType="Done"/>
                        </VerticalStackLayout>

                        <VerticalStackLayout Spacing="5">
                            <Label Text="O mnie" FontSize="12" TextColor="Gray"/>
                            <Entry Text="{Binding Bio}" TextColor="Black" BackgroundColor="White" ReturnType="Done"/>
                        </VerticalStackLayout>
                        
                        <VerticalStackLayout Spacing="5">
                            <Label Text="Nazwa użytkownika" FontSize="12" TextColor="Gray"/>
                            <Entry Text="{Binding Username}" TextColor="Black" BackgroundColor="White" ReturnType="Done"/>
                        </VerticalStackLayout>

                        <Button Text="Zapisz" 
                                BackgroundColor="Black" 
                                TextColor="White" 
                                CornerRadius="20" 
                                HeightRequest="50" 
                                Margin="0, 20, 0, 0"
                                Clicked="OnSaveClicked"/>

                    </VerticalStackLayout>
                </Border>
            </ScrollView>

        </Grid>
    </Grid>
</ContentPage>